#include <stdio.h>

/**
 * mul - This function multiply two numbers
 * @x: fist number
 * @y: second number
 * Author - Thaoban Abdrasheed
 *
 * Return: return multiplications of two numbers
 */
int mul(int x, int y)
{
	return (x * y);
}
